name: Main Branch Workflow

on:
  push:
    branches: [main]

jobs:
  hai:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Run your build commands
        run: |
          echo "Hello World"